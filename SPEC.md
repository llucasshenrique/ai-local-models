# NOTE: retargeted. This is global orca-ide tooling, not game code. Deliverables live in THIS repo root (Modelfiles, scripts) and docs/local-llm-workers.md here; ignore 'tools/local-llm/' and ai-world paths below. T1 Modelfiles/bench already exist (gemma4 ctx 16k/24k/32k all 100% GPU, ~31 tok/s); continue from qwen3 measurements.

# Local LLM workers: tune ollama + opencode (gemma4 / qwen3) and evaluate alternative harnesses

Write English docs/comments. This is TOOLING, not game code: do not touch src/, client/, tests/. Repo deliverables go under tools/local-llm/ and docs/local-llm-workers.md. You MAY edit the user's opencode config ~/.config/opencode/opencode.json but ALWAYS back it up first (cp to opencode.json.bak-<date>) and report the exact diff. No sudo. Never delete or overwrite existing ollama models; only add new tags. Never print API keys/tokens. GPU has 11 GB: load ONE model at a time and unload (ollama stop <model>) when done. Keep every test small and time-boxed (<= 3 min each).

## Goal (user)
Configure a local model to also do our micro-tasks (small, single-file, well specified code tasks with a test command) via opencode + ollama. Currently gemma4 is used and it is inefficient and LOOPS. Improve the LLM parameters for efficiency and to avoid loops, and evaluate whether a better harness than opencode exists for small local models (and configure the best one(s)).

## Findings already gathered (verify, do not blindly trust)
- Hardware: RTX 2080 Ti 11 GB (Turing), 31 GB RAM, 12 cores. ollama 0.31.1 (systemd, override in /etc/systemd/system/ollama.service.d/override.conf with OLLAMA_CONTEXT_LENGTH=16384, OLLAMA_KEEP_ALIVE=30m, OLLAMA_NUM_PARALLEL=1, OLLAMA_MAX_LOADED_MODELS=1; NO flash attention and NO KV cache quantization set).
- Models: gemma4:12b (7.6 GB, Q4_K_M, 262k ctx, capabilities completion/vision/audio/tools/thinking, no Parameters set), gemma4:latest (8B, 9.6 GB, ctx 131072, params temperature 1, top_k 64, top_p 0.95 = Google chat defaults, too hot for agentic tool use), qwen3:14b (9.3 GB), qwen3:4b, qwen2.5:7b, orchestrator:latest (custom qwen3 4B: num_ctx 8192, num_predict 2048, temperature 0.2, top_k 20, top_p 0.9, repeat_penalty 1, stop <|im_start|>/<|im_end|>; a good template).
- Measured: gemma4:12b at num_ctx 16384 runs 100% on GPU, ~31 tok/s generation, 13 s cold load, 9.3 GB VRAM used.
- opencode 1.18.31, config ~/.config/opencode/opencode.json: model ollama/qwen3:14b, small_model ollama/qwen3:4b, provider ollama via @ai-sdk/openai-compatible baseURL http://localhost:11434/v1, models gemma4:latest, gemma4:12b, orchestrator:latest, qwen3:4b, qwen3:14b, NO per-model options/limits, no agent step limits.
- Hypotheses to test: (a) opencode talks to ollama through the OpenAI-compatible /v1 endpoint, which ignores num_ctx/top_k/repeat_penalty, so effective sampling is the model's Modelfile defaults + server env, hence baking parameters into tuned ollama tags (Modelfile PARAMETER) is the reliable way; (b) temperature 1.0 and thinking mode on gemma4 cause loops in tool-use agents; (c) no per-model `limit.context/output` means opencode never compacts correctly for a 16k context and overflows; (d) missing agent `steps` cap and doom-loop protection lets repeated tool calls run away.

## Tasks (do them in order, each small, commit tools/docs as you go)
T1. Tuned model tags via Modelfiles in tools/local-llm/ plus tools/local-llm/create-models.sh (idempotent, only `ollama create`): at least `gemma4-agent:12b` (FROM gemma4:12b) and `qwen3-agent:14b` (FROM qwen3:14b). Start from: temperature 0.2-0.3, top_k 20-40, top_p 0.85-0.9, repeat_penalty 1.05-1.15 with repeat_last_n 256, num_ctx 16384 (test whether 24576/32768 still fits 100% on GPU), num_predict cap ~4096, proper stop tokens for each model's template, presence_penalty via opencode options where supported. For thinking models find out how to keep thinking short or off for micro-tasks (e.g. /no_think for qwen3, gemma4 thinking control) and whether that helps. Justify each value from a quick measurement (tokens/s, GPU fit via `ollama ps`, repetition behaviour), not folklore.
T2. Server env recommendations (do NOT apply, no sudo): test whether OLLAMA_FLASH_ATTENTION=1 and OLLAMA_KV_CACHE_TYPE=q8_0 are worth it on this Turing GPU (you can run a second temporary `OLLAMA_HOST=127.0.0.1:11435 OLLAMA_FLASH_ATTENTION=1 OLLAMA_KV_CACHE_TYPE=q8_0 ollama serve` for the comparison, then kill it). Report exact commands the user should run to apply.
T3. opencode config: add the tuned tags to the ollama provider with per-model `options` (temperature, top_p, and any supported penalty), `limit: { context, output }` matching num_ctx/num_predict, tool_call/reasoning flags if the schema has them, an agent config with a step cap (check the real schema for the current key, e.g. steps/maxSteps) and the doom-loop permission if it exists in opencode 1.18.31 (look at the installed @opencode-ai/sdk types and docs). Set a sensible default model for micro-tasks. Backup first, report diff.
T4. Loop smoke test: run a fixed set of 3 tiny tasks headless with `opencode run` (or its non-interactive equivalent) in a scratch temp git repo (NOT this repo): (1) create a file with a function and make a given shell test pass, (2) rename a symbol across two files, (3) fix a failing assertion. Compare gemma4:12b (untuned), gemma4-agent:12b, qwen3-agent:14b, orchestrator: completion yes/no, wall time, number of tool calls, and whether any repeated-identical-tool-call loop occurred (define and detect it, cap each run at 3 minutes and kill it on loop). Save raw results as a table in the doc.
T5. Harness survey: check which of these are available or cheaply installable without sudo and try the SAME 3 tasks with the best local model on each that works: opencode (baseline), aider (with ollama), goose, crush, qwen-code, codex CLI in --oss mode with ollama, Continue/Cline only if headless usable. Also evaluate a tiny custom minimal tool-loop harness idea (a ~100 line script that calls ollama /api/chat with tools read_file/write_file/run_shell, max 15 steps, repetition guard) as a baseline, since small models often do better with a minimal loop. Recommend the best harness+model pair for our micro-tasks with evidence, and configure it (backup rules as above).
T6. Write docs/local-llm-workers.md: findings, final recommended parameters and why, exact env commands for the user, how to launch a micro-task with the chosen harness, how to plug it into our orchestration (Orca worker via `orca-ide terminal create --command "<harness cmd>"` then `orca-ide orchestration worker-start --terminal <handle> --worktree <sel> --spec ...`, and note that non-Claude workers cannot send worker_done so they report with `orca-ide orchestration send --type status`), limitations, and the results tables.

## Delivery
Commits on your worktree branch (imperative messages). Final worker_done report: what was configured, tuned values with evidence, the results table, the harness recommendation, the exact commands the user must run (sudo/env), the diff of ~/.config/opencode/opencode.json, and open problems. Be honest about anything that failed or could not be measured.
